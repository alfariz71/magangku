import React from 'react';
// GpsSimulatorWidget is disabled in production
// Real GPS is handled via DataContext.gpsState and startGpsWatch/stopGpsWatch
export const GpsSimulatorWidget: React.FC = () => null;
